package view.Profile;

import interface_adapter.Profile.ProfileViewModel;
import view.View;

public class NewPasswordView extends View {

    public NewPasswordView(ProfileViewModel viewModel) {
        super(viewModel);
    }
}
